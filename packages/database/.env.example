# IMPORTANT: Keep this file in sync with ../../.env
# Both .env files must have the same DATABASE_URL for consistency

# Database
DATABASE_URL="postgresql://user:password@localhost:5432/solufacil?schema=public"
